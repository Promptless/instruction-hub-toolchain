{
  "mcpServers": {
    "fixture-docs": {
      "type": "http",
      "url": "https://example.invalid/mcp"
    },
    "fixture-trace": {
      "command": "fixture-trace",
      "args": [
        "--org",
        "${PROMPTLESS_ORG_ID}"
      ],
      "env": {
        "PROMPTLESS_API_KEY": "${PROMPTLESS_API_KEY}"
      }
    }
  }
}
